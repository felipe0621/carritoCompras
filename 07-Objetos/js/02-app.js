//Como acceder a los valores de un objeto
const producto = {
     nombre : "Monitor de 20 pulgadas",
     precio : 300,
     disponible : true,
}

console.log(producto);

console.log(producto.nombre); //otra forma seria la siguiente linea
console.log(producto.precio); //otra forma seria la siguiente linea
console.log(producto.disponible); //otra forma seria la siguiente linea
console.log(producto['nombre']); //otra forma seria la siguiente linea
