//Operador ternario video 79

//operador ternario con una condicion
/*const autenticado = false;

console.log( autenticado ? 'Si está autenticado'  :  'No está autenticado');*/


//revisando dos condiciones en un tenario  con &&
/*const autenticado = true;
const puedePagar = false;

console.log( autenticado && puedePagar? 'Si esta autenticado' : 'NO esta autenticado');*/

//revisando dos condiciones en un tenario  con ||
const autenticado = false;
const puedePagar = false;

console.log( autenticado || puedePagar? 'Si puede pagar' : 'NO esta autenticado');